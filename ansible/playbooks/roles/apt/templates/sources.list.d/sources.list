{% for entry in sources.entries %}
{{ entry.type }}{% if entry.options is defined %} [{{ ' '.join(entry.options) }}]{% endif %} {{ entry.uri }} {{ entry.suite }} {{ ' '.join(entry.components) }}
{% endfor %}
